// script.js
// This is a sample JavaScript file for testing purposes.

function greet(name) {
    const message = `Hello, ${name}!`;
    document.body.innerHTML += `<p>${message}</p>`;
}

// Example usage
greet("Rider");